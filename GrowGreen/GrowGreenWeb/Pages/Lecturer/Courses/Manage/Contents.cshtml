@page
@model GrowGreenWeb.Pages.Lecturer.Courses.Manage.ContentsModel
@{
    ViewData["Title"] = "Contents";
}

<h1>Course Management</h1>
<div class="row">
    @* Sidebar *@
    <div class="col-3">
        <div class="mb-3 card p-2">
            <div class="card-body">
                <h5>@Model.Course.Name</h5>
                <a asp-page="Index" asp-route-id="@Model.Course.Id" class="text-reset text-decoration-none">Edit Course Details</a>
                <hr />
                <h5>
                    <a asp-page="QnA" asp-route-id="@Model.Course.Id" class="text-reset text-decoration-none">Q&amp;A</a>
                </h5>
                <hr />
                <h5>
                    <span href="#" class="text-reset text-decoration-none">
                        Contents&nbsp;
                        <a asp-page="CreateLecture" asp-route-id="@Model.Course.Id" class="text-reset text-decoration-none">(Add)</a>
                    </span>
                </h5>
                @foreach (var lecture in Model.Course.Lectures)
                {
                    <a asp-page="Contents" asp-route-id="@lecture.Course.Id" asp-route-lectureId="@lecture.Id"
                   class="text-reset text-decoration-none">
                        @lecture.Name
                        </a>
                }
                <hr />
                <h5>
                    <a asp-page="Quizzes" asp-route-id="@Model.Course.Id" class="text-reset text-decoration-none">Quizzes</a>
                </h5>
            </div>
        </div>
    </div>

    <div class="col-9">
        <div class="mb-3 card p-4">
            <h3>Lecture X</h3>
        </div>
        @* @foreach (var lecture in Model.Course.Lectures)
        {
        <div class="card mb-3 col-12 px-0">
        <div class="row g-0">
        <div class="col-lg-2">
        @{
        string img;

        //TODO: Add image preview
        img = "/img/courses/no-preview.jpg";

        //if (lecture.ImageUrl is null)
        //{
        //    img = "/img/courses/default-course.png";
        //}
        //else
        //{
        //    img = lecture.ImageUrl;
        //}

        <img src="@img" class="card-img"
        style="object-fit: cover; border-radius: 15px; height: 132.5px;" />
        }
        </div>
        <div class="col-lg-8 ps-3">
        <div class="card-body">
        <h3>@lecture.Name</h3>
        By: <strong>@lecture.Lecturer.FullName</strong>
        <div class="row pt-2">
        <div class="col-6">
        <img src="/img/courses/Clock.png" class="icon" />
        <span>
        Last updated
        <strong>
        @if (lecture.LastUpdatedTimestamp is null)
        {
        <text>Never</text>
        }
        else
        {
        <text>@lecture.LastUpdatedTimestamp.Value.ToString("g")</text>
        }
        </strong>
        </span>
        </div>

        <div class="col-6">
        <img src="/img/courses/Person.png" class="icon" />
        <span><strong>@lecture.Learners.Count</strong> participants</span>
        </div>
        </div>
        </div>
        </div>
        <div class="col-lg-2">
        <div class="card-body h-100 text-center d-grid" style="place-content: center;">
        <a class="text-reset text-decoration-none"
        asp-page="Viewer/Index" asp-route-id="@lecture.Id">
        <img src="/img/icons/right.png" class="icon icon-lg" />
        </a>
        </div>
        </div>
        </div>
        </div>
        }*@
    </div>
</div>
